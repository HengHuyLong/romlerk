import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Base palette
  static const Color white = Color(0xFFF7F7F2);
  static const Color green = Color(0xFF899878);
  static const Color darkGray = Color(0xFF222725);
  static const Color black = Color(0xFF121113);

  // 🛠 Roles (aliases)
  static const Color background = white;
  static const Color primary = darkGray;
  static const Color secondary = green;
  static const Color text = black;
}
